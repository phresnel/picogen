.deb-Packages Only
  [x] Check if suggestions are installable

Installers/Packages Only
  [x] Test whether each program starts through start-/app-menu
  [x] Test whether .picogen-files can be opened via double-click
  [-] Test whether nsis-uninstall is clean

Bare packages only; they don't have to be executable
  [x] Test whether each program starts

SimplexGui with default scene
  [x] Test whether <ctrl>+f5 starts rendering the default scene
  [x] Press <ctrl>+f6. Should result in error message about unsaved data
  [x] Press <ctrl>+s and save the default scene
  [x] Press <ctrl>+f6. Should result in RenderWindow
  [x] tweak something, try <ctrl>+f6 -> error message

SimplexGui with example scene
  [x] Load any of the example scenes
  [x] Test whether <ctrl>+f5 starts rendering the scene
  [x] Press <ctrl>+f6. Should result in error message about unsaved data
  [x] Press <ctrl>+s and save the scene
  [x] Press <ctrl>+f6. Should result in RenderWindow
  [x] tweak something, try <ctrl>+f6 -> error message

SimplexGui with loaded scene
  [x] Load scene
  [x] Press <ctrl>+f5. Should result in internal RenderWindow
  [x] Press <ctrl>+f6. Should result in external RenderWindow
  [x] tweak something, try <ctrl>+f6 -> error message

SimplexGui - Examples
  [x] test whether each example can be rendered in a few seconds in standard settings
