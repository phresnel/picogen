#ifndef REAL_H
#define REAL_H

#include <cmath>
#include <algorithm>

namespace picogen { namespace cracker {

        typedef float real;

        using std::sqrt;
        using std::pow;
        using std::min;
        using std::max;
} }

#endif // REAL_H
