set (sources ${sources}
        ${pwd}/collection.cc
        ${pwd}/database.cc
        ${pwd}/fragment.cc
        ${pwd}/metadata.cc
        ${pwd}/parametricpreset.cc
        ${pwd}/repositorysettings.cc
)
set (forms ${forms}
)
include_directories ("${pwd}/")