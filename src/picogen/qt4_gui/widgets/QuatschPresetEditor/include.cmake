set (sources ${sources}
        ${pwd}/filenameedit.cc
        ${pwd}/quatschpreseteditor.cc
)
set (forms ${forms}
        ${pwd}/filenameedit.ui
        ${pwd}/quatschpreseteditor.ui
)
include_directories ("${pwd}/")
