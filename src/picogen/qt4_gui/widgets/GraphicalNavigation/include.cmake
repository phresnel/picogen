set (sources ${sources}
        ${pwd}/graphicalnavigationwidget.cc
        ${pwd}/navigationgraphicsview.cc
        ${pwd}/observer.cc
        ${pwd}/observergraphicsitem.cc
        ${pwd}/pitchwidget.cc
        ${pwd}/rollwidget.cc
)
set (forms ${forms}
        ${pwd}/graphicalnavigationwidget.ui
)
include_directories ("${pwd}/")